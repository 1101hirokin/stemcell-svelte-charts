# 実験ログ: 仕様の穴

BarChart を Svelte で描いていて「仕様だけからは一意に決まらなかった判断」の記録。分類は svelte 実装リポと同じ。

- [A] Normative の欠落・矛盾 → 手を止めて仕様へ戻す
- [B] Web の表現として正当な選択 → 仮置きを記録。仕様は沈黙のままでよい
- [C] 仕様は決めているが、機械が照合できない → 検査の課題として記録

| # | どこ | 何が決まっていなかったか | 仮置き | 分類 |
|---|---|---|---|---|
| 1 | BarChart | 棒の太さに上限が無い。広い場所に少数のカテゴリを置くと、一本が 91px の板になり、図が塗り絵になる(実測) | 上限(48px)を charts-core が持ち、群は帯の中央に置く。姿の方針「シャープでスリム」(オーナー裁定 2026-07-29)の実装 | A(仕様へ還す。BarChart.md は下限しか書いていない) |
| 2 | BarChart | 送れる領域の名前。CodeBlock は名前を要求するが、図では図そのものが名前を持つまとまりである | 名前を持たせない(同じ名前を二度名乗ると読み上げが重なる)。焦点は受ける(SC 2.1.1) | B |
| 3 | (実装) | 場所を測る対象。契約は「高さは置かれた場所に従う」とだけ言う | 空いている場所(送れる領域)を測る。絵そのものを測ると、溢れた絵が場所を広げ、次の計算で溢れなくなる堂々巡りになり、下限も送りも効かなくなった(実測で判明。最初の実装がこれだった) | B |
| 4 | BarChart | 長い説明(`description`)を目に見える場所へ出すか | 読み上げへ渡すだけにする(`aria-describedby`)。図の隣に文章を置くかは画面の判断で、部品が場所を取ると外せない | B |
| 5 | BarChart | 帯が狭すぎてカテゴリ名が一文字も入らないとき。仕様は「切り詰める」「間引かない」とだけ言う | 切り詰めたまま出す(「1,1,1」と並ぶ)。読めない札を隠すのは間引きの一種なので、実装では決めない | B(仕様の判断待ち) |
| 6 | Svelte | 鍵盤で焦点を移す先を、状態を変えた直後に掴むと、入れ替わる前の要素を掴む。しかも掴んだ相手に `focus()` が無いと例外になり、Svelte 5 は event handler の後に同期で流すため、状態の更新ごと消える(実測。矢印が効かない見た目になる) | `tick()` の後に焦点を移し、`focus?.()` で呼ぶ | B(Svelte の土地の事情。他の実装リポにも効く注意) |
| 7 | BarChart | 系列が 7 つ以上のときの色。分類は 6 段(dataviz §3) | 回して使う(7 番目は 1 段目)。拒むと図が出ない。同じ色が二度出るぶんは直接ラベルと凡例で区別する | B(dataviz §7 の TODO は「回すか拒むか」を各図に残している) |
| 8 | (harness) | 契約との適合を機械で照合していない(svelte リポの conformance に相当するものが無い) | 目視と単体の検査で担保。契約の props / 既定値の照合は harness を移植してから | C |
| 9 | BarChart | 同じデータの表を開いたときに、絵の高さをどうするか。仕様は「表は畳んで置く」「高さは置かれた場所に従う」とだけ言う | 絵は縮めず、開いたぶんは下へ伸びる(オーナー裁定 2026-07-29。最初の実装は場所の中へ収めようとして絵がひしゃげた)。固定高さの場所に置くと外へ出るので、README で `ratio` か成り行きの高さを勧める | A(仕様へ還す) |
| 11 | (実装) | 外枠(名前・凡例・軸の札・送れる領域・表)を図ごとに書くか、共有するか | internal/Frame.svelte へ共有した。図が増えるたびに同じ骨組みが増えるのを避ける。向き(縦棒/横棒)は Frame が受け、札の並べ先を切り替える。棒だけの部分(棒そのもの・値の札・描き込み)は internal/bar.css に残す | B |
| 12 | (実装) | 読みの向きの取り方。SVG の座標は物理で、札は論理プロパティで置くので、放っておくと絵と札が逆を向く | RTL では絵の x を鏡にする(札の自動反転と揃う)。向きは環境から読み、後から変わることがあるので dir / lang を見張る。derived に置くと要素が結ばれた後に読み直されず false のままだった(実測) | B |
| 13 | LineChart / AreaChart | 描き込みの動き。仕様は「初回の描き込み」とだけ言う | 線が描かれる方向へ伸びる(棒のように下からせり上がる形は採らない。オーナー、2026-07-29)。SVG の g へ CSS の clip-path: inset() を当てても効かなかった(実測)ので、clipPath の矩形を広げる。RTL では右から左へ拭く | A(仕様へ還す。dataviz か各図に「描き込みは描く方向へ」と書く) |
| 14 | (harness) | 契約との適合を機械で照合していなかった(#8 の解消) | svelte リポの conformance を移植した。図では CSS が部品ごとに一枚ではない(外枠は internal が持つ)ので、必須トークンは「その図の CSS と internal の CSS すべて」から探す。基底の Chart は実装を持たないので、meta.ts のある契約だけ見る | C(解消済み) |
| 15 | (実装) | 分類の色の参照の置き場所。JS が var(--color-dataviz-categorical-N) を組み立てていたので、CSS に現れず適合が測れなかった | CSS に別名(--sc-chart-series-N)を一箇所置き、JS は段だけ選ぶ。図が色を決めないという線を、参照の置き場所でも守る形になった | B |
| 16 | PieChart | 名前が「入る」かの判定。仕様は「入らない扇には出さない」とだけ言う | 引き出した先の半径での弧の長さが一行(16px)に足りるかで決める。足りない扇には出さない(値は表と、指したときの表示で届く) | B |
| 17 | ScatterChart / PieChart | 描き込みの動き。仕様は「その図が描かれる方向へ」とだけ言い、点に順序の無い図と角の図をどうするかは書いていない | 散布はそれぞれの点がその場所でふくらむ(順番を付けない: 原点から順に現れる形は、無いかもしれない関係を感じさせる。オーナー裁定 2026-07-29)。円は 0(真上)から時計回りに掃き、RTL では図ごと鏡にする | A(仕様へ還す) |
| 18 | (harness) | 必須トークンを探す CSS の範囲。internal を丸ごと見ると、その部品が読んでいない CSS まで数える(小さな線が円の字の役を要求されて赤くなった) | import を辿って「その部品が実際に読む CSS」を集める(conformance/css.ts) | C(解消済み) |
| 19 | (実装) | SVG の g へ CSS の mask-image を当てても効かない(clip-path: inset() と同じ罠)。計算値は入るのに絵は変わらない | 掃きは HTML の器に掛ける(SVG を包む div へ conic-gradient の mask)。実測で、35ms の時点で最初の扇だけが出ている | B |
